import 'package:flutter/material.dart';

class AnimationTransformRotate extends StatelessWidget {
  const AnimationTransformRotate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
